module.exports = {
    login: (req, res)=>{
        const dbi = req.app.get('db');
    },
    register: (req, res)=>{
        const dbi = req.app.get('db');
    },
    logout: (req,res)=>{
        const dbi = req.app.get('db');
        
    }
}